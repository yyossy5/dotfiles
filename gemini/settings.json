{
  "theme": "ANSI",
  "selectedAuthType": "oauth-personal",
  "mcpServers": {
    "atlassian": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-remote",
        "https://mcp.atlassian.com/v1/sse"
      ]
    }
  },
  "preferredEditor": "vim",
  "excludeTools": [
    "ShellTool(rm -rf)",
    "ShellTool(sudo dd)",
    "ShellTool(sudo mkfs)",
    "ShellTool(sudo fdisk)",
    "ShellTool(sudo mount)",
    "ShellTool(sudo umount)",
    "ShellTool(dd)",
    "ShellTool(mkfs)",
    "ShellTool(fdisk)",
    "ShellTool(chmod 777 /*)",
    "ShellTool(chown root)",
    "ShellTool(sudo chmod 777)",
    "ShellTool(sudo chown)",
    "ShellTool(sudo -i)",
    "ShellTool(sudo su)",
    "ShellTool(docker system prune -af)",
    "ShellTool(npm publish)",
    "ShellTool(cargo publish)",
    "ShellTool(git commit)",
    "ShellTool(git push)"
  ]
}